services:
  - type: web
    name: marketplace-frontend
    runtime: static
    buildCommand: npm install; rm -rf dist; npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /assets/*
        destination: /assets/*
      - type: rewrite
        source: /*
        destination: /index.html
